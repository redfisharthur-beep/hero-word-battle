import { readFile, writeFile } from 'node:fs/promises'

async function patchFile(path, patches){
  let text=await readFile(path,'utf8')
  for(const [label,from,to] of patches){
    if(text.includes(to)) continue
    if(!text.includes(from)) throw new Error(`${path}: patch target not found: ${label}`)
    text=text.replace(from,to)
  }
  await writeFile(path,text,'utf8')
}

await patchFile('worker/index.ts',[
  ['player quiz fields',
`type Player={id:string;name:string;host:boolean;jobId?:string;hp:number;maxHp:number;atk:number;def:number;alive:boolean;guard:boolean;guardCharges?:number;guardMultiplier?:number;ultimateUsed?:boolean;action?:ActionName;answered?:boolean;coefficient?:number;answeredAt?:number;answerCorrect?:boolean;lastSeen?:number;questionsSeen?:number;correctAnswers?:number;damageDone?:number;healingDone?:number}`,
`type Player={id:string;name:string;host:boolean;jobId?:string;hp:number;maxHp:number;atk:number;def:number;alive:boolean;guard:boolean;guardCharges?:number;guardMultiplier?:number;ultimateUsed?:boolean;action?:ActionName;answered?:boolean;coefficient?:number;answeredAt?:number;answerCorrect?:boolean;quizIndex?:number;roundCorrect?:number;roundAnswered?:number;quizQuestionStartedAt?:number;lastSeen?:number;questionsSeen?:number;correctAnswers?:number;damageDone?:number;healingDone?:number}`],
  ['room question list',
`type RoomState={roomId:string;phase:Phase;battlePhase:BattlePhase;round:number;questionIndex:number;usedQuestionIds?:number[];question?:PublicQuestion;wordPoolSize:WordPoolSize;jobsEndsAt?:number;actionEndsAt?:number;quizEndsAt?:number;resolveEndsAt?:number;resolveQueue?:string[];resolveIndex?:number;currentResult?:ActionResult;players:Player[];log:string[]}`,
`type RoomState={roomId:string;phase:Phase;battlePhase:BattlePhase;round:number;questionIndex:number;usedQuestionIds?:number[];question?:PublicQuestion;questions?:PublicQuestion[];wordPoolSize:WordPoolSize;jobsEndsAt?:number;actionEndsAt?:number;quizEndsAt?:number;resolveEndsAt?:number;resolveQueue?:string[];resolveIndex?:number;currentResult?:ActionResult;players:Player[];log:string[]}`],
  ['quiz duration',`const ACTION_MS=5000,QUIZ_MS=8000,JOBS_MS=30000,RESOLVE_STEP_MS=5000,PRESENCE_MS=45000,SWEEP_MS=15000`,`const ACTION_MS=5000,QUIZ_MS=15000,JOBS_MS=30000,RESOLVE_STEP_MS=5000,PRESENCE_MS=45000,SWEEP_MS=15000`],
  ['decimal coefficient',`const clampCoefficient=(v:number)=>Math.max(0,Math.min(4,Math.floor(v)))`,`const clampCoefficient=(v:number)=>Math.max(0,Math.min(6,Math.round(v*100)/100))\nconst roundCoefficient=(elapsedMs:number,correct:number)=>{const base=elapsedMs<=5000?4:elapsedMs<=10000?3:2;const accuracy=correct>=3?1.5:correct===2?1.25:correct===1?1:0;return clampCoefficient(base*accuracy)}`],
  ['clear questions',` private clearBattleFlow(s:RoomState){s.question=undefined;s.jobsEndsAt=undefined;s.actionEndsAt=undefined;s.quizEndsAt=undefined;s.resolveEndsAt=undefined;s.resolveQueue=undefined;s.resolveIndex=undefined;s.currentResult=undefined}`,
` private clearBattleFlow(s:RoomState){s.question=undefined;s.questions=undefined;s.jobsEndsAt=undefined;s.actionEndsAt=undefined;s.quizEndsAt=undefined;s.resolveEndsAt=undefined;s.resolveQueue=undefined;s.resolveIndex=undefined;s.currentResult=undefined}`],
  ['begin action reset',` private beginAction(s:RoomState){s.battlePhase='action';s.question=undefined;s.quizEndsAt=undefined;s.resolveEndsAt=undefined;s.resolveQueue=undefined;s.resolveIndex=undefined;s.currentResult=undefined;s.actionEndsAt=Date.now()+ACTION_MS;for(const p of s.players){p.action=undefined;p.answered=false;p.coefficient=undefined;p.answeredAt=undefined;p.answerCorrect=undefined}}`,
` private beginAction(s:RoomState){s.battlePhase='action';s.question=undefined;s.questions=undefined;s.quizEndsAt=undefined;s.resolveEndsAt=undefined;s.resolveQueue=undefined;s.resolveIndex=undefined;s.currentResult=undefined;s.actionEndsAt=Date.now()+ACTION_MS;for(const p of s.players){p.action=undefined;p.answered=false;p.coefficient=undefined;p.answeredAt=undefined;p.answerCorrect=undefined;p.quizIndex=0;p.roundCorrect=0;p.roundAnswered=0;p.quizQuestionStartedAt=undefined}}`],
  ['start battle no pre-pick',` private startBattle(s:RoomState){s.phase='battle';s.jobsEndsAt=undefined;s.round=1;this.pickQuestion(s);s.log=[\`戰鬥開始！題庫：\${s.wordPoolSize} 字\`];`,
` private startBattle(s:RoomState){s.phase='battle';s.jobsEndsAt=undefined;s.round=1;s.log=[\`戰鬥開始！題庫：\${s.wordPoolSize} 字｜每回合 3 題、每題 5 秒\`];`],
  ['prepare three questions',` private prepareQuiz(s:RoomState){s.battlePhase='quiz';s.actionEndsAt=undefined;s.resolveEndsAt=undefined;s.currentResult=undefined;s.quizEndsAt=Date.now()+QUIZ_MS;s.question=publicQuestion(this.currentQuestion(s));for(const p of s.players){if(p.alive)p.questionsSeen=(p.questionsSeen??0)+1;p.answered=!p.alive;p.coefficient=p.alive?undefined:0;p.answeredAt=undefined;p.answerCorrect=p.alive?undefined:false}}`,
` private prepareQuiz(s:RoomState){s.battlePhase='quiz';s.actionEndsAt=undefined;s.resolveEndsAt=undefined;s.currentResult=undefined;const started=Date.now();s.quizEndsAt=started+QUIZ_MS;const qs:PublicQuestion[]=[];for(let i=0;i<3;i++){this.pickQuestion(s);qs.push(publicQuestion(this.currentQuestion(s)))}s.questions=qs;s.question=qs[0];for(const p of s.players){p.answered=!p.alive;p.coefficient=p.alive?undefined:0;p.answeredAt=undefined;p.answerCorrect=p.alive?undefined:false;p.quizIndex=0;p.roundCorrect=0;p.roundAnswered=0;p.quizQuestionStartedAt=p.alive?started:undefined}}`],
  ['replace old coefficient method',` private coefficientFromDeadline(s:RoomState){if(!s.quizEndsAt)return 0;const n=Math.max(0,Math.ceil((s.quizEndsAt-Date.now())/1000));return n>=7?4:n>=5?3:n>=3?2:n>=1?1:0}`,
` private finishPlayerQuiz(p:Player,s:RoomState,when=Date.now()){p.answered=true;p.answeredAt=when;const elapsed=Math.max(0,QUIZ_MS-Math.max(0,(s.quizEndsAt??when)-when));p.coefficient=roundCoefficient(elapsed,p.roundCorrect??0);p.answerCorrect=(p.roundCorrect??0)>0;p.quizIndex=3;p.quizQuestionStartedAt=undefined}`],
  ['start resolve clears question list',` private startResolve(s:RoomState){s.battlePhase='resolve';s.quizEndsAt=undefined;s.question=undefined;`,
` private startResolve(s:RoomState){s.battlePhase='resolve';s.quizEndsAt=undefined;s.question=undefined;s.questions=undefined;`],
  ['finish round no pre-pick',`s.round++;this.pickQuestion(s);this.beginAction(s)`,`s.round++;this.beginAction(s)`],
  ['quiz timeout logic',`if(s.battlePhase==='quiz'&&s.quizEndsAt&&now>=s.quizEndsAt){const endTime=s.quizEndsAt;for(const p of s.players.filter(p=>p.alive))if(!p.answered){p.answered=true;p.coefficient=0;p.answerCorrect=false;p.answeredAt=endTime+1}s.log.push('8 秒答題結束，依作答完成順序開始行動。');this.startResolve(s);return true}`,
`if(s.battlePhase==='quiz'&&s.quizEndsAt){if(now>=s.quizEndsAt){const endTime=s.quizEndsAt;for(const p of s.players.filter(p=>p.alive&&!p.answered)){const remaining=Math.max(0,3-(p.roundAnswered??0));p.questionsSeen=(p.questionsSeen??0)+remaining;p.roundAnswered=3;this.finishPlayerQuiz(p,s,endTime)}s.log.push('15 秒三題挑戰結束，依完成順序開始行動。');this.startResolve(s);return true}let changed=false;for(const p of s.players.filter(p=>p.alive&&!p.answered)){if(p.quizQuestionStartedAt&&now-p.quizQuestionStartedAt>=5000){p.questionsSeen=(p.questionsSeen??0)+1;p.roundAnswered=(p.roundAnswered??0)+1;p.quizIndex=(p.quizIndex??0)+1;changed=true;if((p.roundAnswered??0)>=3)this.finishPlayerQuiz(p,s,now);else p.quizQuestionStartedAt=now}}if(s.players.filter(p=>p.alive).every(p=>p.answered)){this.startResolve(s);return true}if(changed)return true}`],
  ['answer endpoint',`if(url.pathname==='/answer'&&request.method==='POST'){if(this.advanceTimeouts(s)){await this.save(s);return json(s)}if(s.phase!=='battle'||s.battlePhase!=='quiz')return json({error:'Not accepting answers'},409);const b=await request.json() as {playerId?:string;choice?:string},p=s.players.find(x=>x.id===b.playerId);if(!p?.alive)return json({error:'Player not available'},404);if(p.answered)return json({error:'Already answered'},409);p.lastSeen=Date.now();const q=this.currentQuestion(s),correct=b.choice===q.answer;p.answered=true;p.answerCorrect=correct;if(correct)p.correctAnswers=(p.correctAnswers??0)+1;p.coefficient=correct?this.coefficientFromDeadline(s):0;p.answeredAt=Date.now();await this.save(s);return json(s)}`,
`if(url.pathname==='/answer'&&request.method==='POST'){if(this.advanceTimeouts(s)){await this.save(s);return json(s)}if(s.phase!=='battle'||s.battlePhase!=='quiz')return json({error:'Not accepting answers'},409);const b=await request.json() as {playerId?:string;choice?:string},p=s.players.find(x=>x.id===b.playerId);if(!p?.alive)return json({error:'Player not available'},404);if(p.answered)return json({error:'Already answered'},409);const slot=Math.max(0,Math.min(2,p.quizIndex??0)),publicQ=s.questions?.[slot];if(!publicQ)return json({error:'Question unavailable'},409);const q=getVocabularyQuestion(s.wordPoolSize,publicQ.id-1),correct=b.choice===q.answer,now=Date.now();p.lastSeen=now;p.questionsSeen=(p.questionsSeen??0)+1;p.roundAnswered=(p.roundAnswered??0)+1;if(correct){p.roundCorrect=(p.roundCorrect??0)+1;p.correctAnswers=(p.correctAnswers??0)+1}p.quizIndex=slot+1;if((p.roundAnswered??0)>=3)this.finishPlayerQuiz(p,s,now);else p.quizQuestionStartedAt=now;if(s.players.filter(x=>x.alive).every(x=>x.answered))this.startResolve(s);await this.save(s);return json({...s,answerFeedback:{correct,correctAnswer:q.answer,questionNumber:slot+1}})}`]
])

await patchFile('src/App.tsx',[
  ['derive personal quiz question',
`const selectedRoom=useMemo(()=>rooms.find(r=>r.id===roomId),[roomId]);const me=roomState?.players.find(p=>p.id===playerId);const question=roomState?.question??fallbackQuestion;const sortedResults=`,
`const selectedRoom=useMemo(()=>rooms.find(r=>r.id===roomId),[roomId]);const me=roomState?.players.find(p=>p.id===playerId);const question=roomState?.questions?.[Math.min(2,me?.quizIndex??0)]??roomState?.question??fallbackQuestion;const quizQuestionNumber=Math.min(3,(me?.quizIndex??0)+1);const sortedResults=`],
  ['personal five second deadline',
`const deadline=roomState?.battlePhase==='quiz'?roomState.quizEndsAt:roomState?.battlePhase==='resolve'?roomState.resolveEndsAt:roomState?.actionEndsAt;const remainingSeconds=deadline?Math.max(0,Math.ceil((deadline-clock)/1000)):roomState?.battlePhase==='quiz'?8:roomState?.battlePhase==='action'?5:0;`,
`const deadline=roomState?.battlePhase==='quiz'?(me?.quizQuestionStartedAt?me.quizQuestionStartedAt+5000:roomState.quizEndsAt):roomState?.battlePhase==='resolve'?roomState.resolveEndsAt:roomState?.actionEndsAt;const remainingSeconds=deadline?Math.max(0,Math.ceil((deadline-clock)/1000)):roomState?.battlePhase==='quiz'?5:roomState?.battlePhase==='action'?5:0;`],
  ['speech current personal question',
`useEffect(()=>{if(page!=='battle'||roomState?.battlePhase!=='quiz'||!roomState.question||!me?.alive)return;const key=\`${roomState.round}:\${roomState.question.id}\`;if(spokenQuestionRef.current===key)return;spokenQuestionRef.current=key;const t=window.setTimeout(()=>{if(!('speechSynthesis'in window))return;window.speechSynthesis.cancel();const u=new SpeechSynthesisUtterance(roomState.question!.word);u.lang='en-US';u.rate=.82;window.speechSynthesis.speak(u)},120);return()=>window.clearTimeout(t)},[page,roomState?.round,roomState?.battlePhase,roomState?.question?.id,me?.alive])`,
`useEffect(()=>{if(page!=='battle'||roomState?.battlePhase!=='quiz'||!question?.id||!me?.alive||me?.answered)return;const key=\`${roomState.round}:\${question.id}\`;if(spokenQuestionRef.current===key)return;spokenQuestionRef.current=key;const t=window.setTimeout(()=>{if(!('speechSynthesis'in window))return;window.speechSynthesis.cancel();const u=new SpeechSynthesisUtterance(question.word);u.lang='en-US';u.rate=.82;window.speechSynthesis.speak(u)},120);return()=>window.clearTimeout(t)},[page,roomState?.round,roomState?.battlePhase,question.id,me?.alive,me?.answered])`],
  ['reset chosen per subquestion',`useEffect(()=>setSelectedChoice(''),[roomState?.question?.id,roomState?.round])`,`useEffect(()=>setSelectedChoice(''),[question.id,roomState?.round])`],
  ['quiz progress label',`<div className=\"word-line\"><strong className=\"word\">{question.word}</strong></div>`,`<div className=\"word-line\"><span className=\"quiz-progress\">第 {quizQuestionNumber} / 3 題</span><strong className=\"word\">{question.word}</strong></div>`]
])

await patchFile('src/lib/multiplayer.ts',[
  ['remote player quiz fields',
`export type RemotePlayer = { id:string; name:string; host:boolean; jobId?:string; hp:number; maxHp:number; atk:number; def:number; alive:boolean; guard:boolean; guardCharges?:number; guardMultiplier?:number; ultimateUsed?:boolean; action?:RemoteAction; answered?:boolean; coefficient?:number; answeredAt?:number; answerCorrect?:boolean; questionsSeen?:number; correctAnswers?:number; damageDone?:number; healingDone?:number }`,
`export type RemotePlayer = { id:string; name:string; host:boolean; jobId?:string; hp:number; maxHp:number; atk:number; def:number; alive:boolean; guard:boolean; guardCharges?:number; guardMultiplier?:number; ultimateUsed?:boolean; action?:RemoteAction; answered?:boolean; coefficient?:number; answeredAt?:number; answerCorrect?:boolean; quizIndex?:number; roundCorrect?:number; roundAnswered?:number; quizQuestionStartedAt?:number; questionsSeen?:number; correctAnswers?:number; damageDone?:number; healingDone?:number }`],
  ['remote room question list',
`export type RemoteRoomState = { roomId:string; phase:'lobby'|'jobs'|'battle'|'result'; battlePhase:'action'|'quiz'|'resolve'; round:number; questionIndex:number; usedQuestionIds?:number[]; question?:RemoteQuestion; wordPoolSize:WordPoolSize;`,
`export type RemoteRoomState = { roomId:string; phase:'lobby'|'jobs'|'battle'|'result'; battlePhase:'action'|'quiz'|'resolve'; round:number; questionIndex:number; usedQuestionIds?:number[]; question?:RemoteQuestion; questions?:RemoteQuestion[]; wordPoolSize:WordPoolSize;`]
])

console.log('Three-question round rules patched: 3 questions, 5s each, speed + accuracy coefficients.')
